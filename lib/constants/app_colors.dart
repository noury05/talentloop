import 'package:flutter/material.dart';

class AppColors {
  // Main teal color (can be used for buttons and primary accents)
  static const Color teal = Color(0xFF00897B); // similar to Colors.teal[600]

  // Lighter teal background for soft UI surfaces (used for card background)
  static const Color tealShade50 = Color(0xFFE0F2F1); // similar to Colors.teal[50]

  // Mid-range teal (used for borders or subtle highlights)
  static const Color tealShade300 = Color(0xFF4DB6AC); // similar to Colors.teal[300]

  /// Very pale teal (Material.teal[100])
  static const Color tealShade100 = Color(0xFFB2DFDB);

  /// Light teal (Material.teal[200])
  static const Color tealShade200 = Color(0xFF80CBC4);

  // Coral for CTAs (like login button)
  static const Color coral = Color(0xFFF6B24C);

  // General white background
  static const Color white = Color(0xFFFFFFFF);

  // Dark teal for headers or strong emphasis
  static const Color darkTeal = Color(0xFF00695C);

  // Slightly creamy white for card backgrounds
  static const Color softCream = Color(0xFFFFF8F0);

  // Muted red for warnings or subtle alerts
  static const Color mutedRed = Color(0xFFEF9A9A); // similar to Colors.red[200]

  // Light teal for soft UI accents
  static const Color lightTeal = Color(0xFFB2DFDB); // similar to Colors.teal[100]


}
